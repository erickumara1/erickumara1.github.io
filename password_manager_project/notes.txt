# show 3 entries of table
# cursor.execute("SELECT * FROM vault_table LIMIT 3")
# rows = cursor.fetchall()
# print(rows)
# cursor.execute("INSERT INTO vault_table VALUES('launch_url','launch_username','launch_password')")
# cursor.connection.commit()
  